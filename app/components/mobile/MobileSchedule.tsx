"use client";

// Stub for Milestone 3 — the Agenda List View (per the approved mockup's
// "Alternate Mobile Views" option A) is built out in a later milestone.
export default function MobileSchedule() {
  return (
    <div className="flex-1 min-h-0 flex items-center justify-center text-sm text-slate-400 px-6 text-center">
      Schedule (Agenda view) — coming in a later milestone.
    </div>
  );
}
